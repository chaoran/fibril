AM_CPPFLAGS = -I$(includedir)
AM_LDFLAGS = -L$(libdir)
LIBS += -l$(PACKAGE)

check_PROGRAMS = \
                 fib \
                 integrate \
                 matmul \
                 quicksort

TESTS = $(check_PROGRAMS)
